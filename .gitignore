node_modules/
.DS_Store
*.log
.claude/

# Temp / utility files
iconic-grid-html.txt
iconic-svg-data.json
new-icon-grid.txt
plan.md
replace-svgs.js
src/

# package.json, package-lock.json, build/, dist/, source/ are COMMITTED.
# The token build (build/*.mjs) is author-time only; consumers read committed dist/.
